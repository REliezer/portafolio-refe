---
import type { ContactEntry } from "@/types";
import EntryLayout from "@components/contact/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("contact")) as ContactEntry;
---

<EntryLayout entry={entry} />
